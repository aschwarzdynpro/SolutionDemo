if (typeof (ETO) === "undefined") { ETO = {}; };

ETO.BookingCommands = {
    
    printReport: function(formContext) {

        var ExecuteWorkflowRequest = function(entityId, workflowId) {
            this.EntityId = { "guid" : entityId };
            this.entity = {
                id: workflowId,
                entityType: "workflow"
            };
            this.getMetadata = function() {
                return {
                    boundParameter: "entity",
                    parameterTypes: {
                        "entity": {
                            "typeName": "Microsoft.Dynamics.CRM.workflow",
                            "structuralProperty": 5
                        },
                        "EntityId": {
                            "typeName": "Edm.Guid",
                            "structuralProperty": 1
                        }
                    },
                    operationType: 0,
                    operationName: "ExecuteWorkflow"
                };
            };
        };

        var workflowId = "40ae912e-8c56-407f-908c-95dabc199506";
        var wfRequest = new ExecuteWorkflowRequest(Xrm.Page.data.entity.getId(), workflowId);
        var uniqueMessageId = "serviceprintmessage";

        var confirmStrings = { text:"Should the service report be printed?", title:"Print service report" };
        var confirmOptions = { height: 200, width: 250 };
        Xrm.Navigation.openConfirmDialog(confirmStrings, confirmOptions).then(
        function (success) {    
            if (success.confirmed)
            {
                Xrm.WebApi.execute(wfRequest).then(
                    function successCallback(result) {
                        if (result.ok) {
                            result.json().then(function(respo) {
                                formContext.ui.setFormNotification("The service report is printed.", "INFO", uniqueMessageId);
                            });
                        };
                    },
                    function errorCallback(error) {
                        Xrm.Navigation.openErrorDialog({ message:error.message });
                    }
                )
            }
        });  
    },

    printReportAction: function(formContext, sendEmail) {
        var uniqueMessageId = "bookingprintmessage";

        var confirmStrings = { text:"Should the service report be printed?", title:"Service message" };
        var confirmOptions = { height: 200, width: 250 };
        Xrm.Navigation.openConfirmDialog(confirmStrings, confirmOptions).then(
        function (success) {    
            if (success.confirmed)
            {
                formContext.getAttribute("hso_lastprintdate_dat").setValue(new Date());
                formContext.getAttribute("hso_af_sendemail_bol").setValue(sendEmail);
                formContext.data.save();
                
                var notification = 
                {
                    type: 1,
                    level: 4, // information
                    message: "Printing has started."
                }
                Xrm.App.addGlobalNotification(notification);
            }
        });  
    }
};