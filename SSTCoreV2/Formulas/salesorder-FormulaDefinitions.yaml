sst_profitcontributionmargin_calc: =100 * Decimal(sst_profit_cur) / Decimal(totalamountlessfreight)
sst_total_discount_calc: =100 * ((Decimal(totallineitemamount) - Decimal(totalamountlessfreight)) / Decimal(totallineitemamount))
